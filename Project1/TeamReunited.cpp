#include "Menu.h"


/*#include <gtest/gtest.h>
struct test{
	int balance = 0;
	test(char * = nullptr){}
	explicit test(const int balance)
	: balance{balance}{	} };
TEST(SomethingTest, TestStartsEmpty) {
	test something;
	EXPECT_EQ(0, something.balance);
}
int test(int argc, char* argv[]) {	
	testing::InitGoogleTest(&argc, argv);
	return RUN_ALL_TESTS();
}*/

int main()
{
	menu();
	
}
